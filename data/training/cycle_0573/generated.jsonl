{"instruction": "Create a Python class that implements a thread-safe queue using threading.Lock", "output": "[Cloud error: The read operation timed out]", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Create a Python function to encrypt and decrypt data using Fernet", "output": "Berikut implementasi enkripsi dan dekripsi menggunakan Fernet (symmetric encryption) dari library `cryptography`. Fernet menjamin *confidentiality* dan *integrity* data.\n\n### Prasyarat\nInstall library terlebih dahulu:\n```bash\npip install cryptography\n```\n\n### Implementasi Kode\n```python\nfrom cryptography.fernet import Fernet\nimport os\n\nclass SecurityCrypto:\n    def __init__(self, key: bytes = None):\n        if key is None:\n            raise ValueError(\"Kunci enkripsi wajib disediakan. Jangan generate baru setiap saat untuk data existing.\")\n        self.cipher = Fernet(key)\n\n    @staticmethod\n    def generate_key() -> bytes:\n        \"\"\"Generate kunci baru. Simpan ini di secure vault/environment variable.\"\"\"\n        return Fernet.generate_key()\n\n    def encrypt_data(self, data: str) -> bytes:\n        \"\"\"Enkripsi string menjadi bytes.\"\"\"\n        if not isinstance(data, str):\n            raise TypeError(\"Data harus berupa string.\")\n        return self.cipher.encrypt(data.encode('utf-8'))\n\n    def decrypt_data(self, token: bytes) -> str:\n        \"\"\"Dekripsi bytes kembali menjadi string.\"\"\"\n        try:\n            decrypted = self.cipher.decrypt(token)\n            return decrypted.decode('utf-8')\n        except Exception as e:\n            # Log error ini untuk audit keamanan\n            raise ValueError(f\"Dekripsi gagal. Token mungkin rusak atau kunci salah: {e}\")\n\n# --- Contoh Penggunaan ---\nif __name__ == \"__main__\":\n    # 1. Generate kunci (LAKUKAN SEKALI SAJA, lalu simpan aman)\n    key = SecurityCrypto.generate_key()\n    print(f\"Kunci Rahasia: {key.decode()}\") \n\n    # 2. Inisialisasi\n    crypto = SecurityCrypto(key)\n\n    # 3. Enkripsi\n    original_message = \"Data sensitif PT Sherif Network\"\n    encrypted = crypto.encrypt_data(original_message)\n    print(f\"Enkripsi: {encrypted}\")\n\n    # 4. Dekripsi\n    decrypted = crypto.decrypt_data(encrypted)\n    print(f\"Dekripsi: {decrypted}\")\n```\n\n### Catatan Keamanan Kritis\n1.  **Key Management:** Jangan pernah hardcode kunci di sumber kode (source code). Gunakan Environment Variables atau secret management system (seperti HashiCorp Vault, AWS Secrets Manager).\n2.  **Rotasi Kunci:** Fernet tidak mendukung rotasi kunci secara native pada token yang sudah terenkripsi. Jika kunci bocor, data lama tidak bisa diselamatkan kecuali Anda memiliki mekanisme enkripsi ganda atau re-enkripsi.\n3.  **Integritas:** Fernet sudah mencakup HMAC untuk memverifikasi integritas data. Jika token diubah sedikit saja, dekripsi akan gagal (`InvalidToken`).\n4.  **Logging:** Jangan pernah log data asli atau kunci enkripsi. Log hanya status operasi (success/fail).\n\nGunakan ini untuk mengamankan konfigurasi sensitif, token sesi, atau data pribadi pengguna di database. Jika butuh implementasi untuk skala enterprise, hubungi tim infrastruktur PT Sherif Network.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Create a Python utility to normalize and validate file paths to prevent directory traversal", "output": "Ini adalah utilitas Python standar industri untuk mitigasi Directory Traversal (CWE-22). Jangan pernah trusting user input secara langsung untuk operasi file system.\n\nScript ini menggunakan `os.path.realpath` untuk resolusi symlink absolut dan validasi prefix ketat.\n\n```python\nimport os\nimport logging\nfrom pathlib import Path\n\n# Konfigurasi Logging untuk Audit Trail\nlogging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')\nlogger = logging.getLogger(\"ShiroAI_Security\")\n\nclass SecurePathHandler:\n    def __init__(self, base_directory: str):\n        \"\"\"\n        Inisialisasi handler dengan direktori basis yang aman.\n        Direktori basis harus ada di sistem file.\n        \"\"\"\n        self.base_directory = os.path.realpath(base_directory)\n        if not os.path.isdir(self.base_directory):\n            raise FileNotFoundError(f\"Base directory tidak ditemukan: {base_directory}\")\n        \n        logger.info(f\"SecurePathHandler initialized. Base: {self.base_directory}\")\n\n    def validate_path(self, user_input: str) -> str:\n        \"\"\"\n        Normalisasi dan validasi path input pengguna.\n        Mencegah traversal keluar dari base_directory.\n        \n        :param user_input: Path relatif dari pengguna (contoh: 'uploads/file.txt' atau '../../etc/passwd')\n        :return: Absolute path yang aman jika valid.\n        :raises ValueError: Jika terdeteksi upaya directory traversal.\n        \"\"\"\n        if not user_input:\n            raise ValueError(\"Input path tidak boleh kosong.\")\n\n        # Gabungkan base directory dengan input user\n        # os.path.join menangani separator otomatis\n        joined_path = os.path.join(self.base_directory, user_input)\n        \n        # Normalisasi path:.resolve .., ., dan symlink jika file sudah ada\n        # Gunakan abspath jika file belum ada (untuk operasi write/create)\n        # Namun untuk keamanan maksimal terhadap traversal, abspath cukup setelah join\n        normalized_path = os.path.abspath(joined_path)\n\n        # Validasi ketat: Path hasil harus dimulai dengan base_directory + separator\n        # Menambahkan separator mencegah bypass seperti /base_dir_evil ketika base adalah /base_dir\n        if not normalized_path.startswith(self.base_directory + os.sep) and normalized_path != self.base_directory:\n            logger.warning(f\"Attempted directory traversal detected: {user_input} -> {normalized_path}\")\n            raise ValueError(\"Akses ditolak: Directory traversal terdeteksi.\")\n\n        logger.info(f\"Path validated successfully: {normalized_path}\")\n        return normalized_path\n\n# --- Contoh Penggunaan ---\nif __name__ == \"__main__\":\n    # Asumsikan ini direktori aman yang dimiliki aplikasi\n    SAFE_BASE = \"/var/www/uploads\" \n    \n    # Buat direktori dummy untuk testing jika belum ada\n    os.makedirs(SAFE_BASE, exist_ok=True)\n\n    handler = SecurePathHandler(SAFE_BASE)\n\n    test_cases = [\n        \"document.pdf\",             # Valid\n        \"folder/image.png\",         # Valid\n        \"../etc/passwd\",            # INVALID (Traversal)\n        \"../../etc/shadow\",         # INVALID (Traversal)\n        \"....//....//etc/passwd\",   # INVALID (Traversal disguised)\n        \"/etc/passwd\",              # INVALID (Absolute path injection)\n    ]\n\n    for case in test_cases:\n        try:\n            safe_path = handler.validate_path(case)\n            print(f\"[OK] {case} -> {safe_path}\")\n        except ValueError as e:\n            print(f\"[BLOCKED] {case} -> {str(e)}\")\n```\n\n### Analisis Teknis & Mitigasi\n\n1.  **`os.path.realpath` pada Base**:\n    *   Memastikan direktori basis sudah resolusi penuh (menghilangkan symlink yang bisa mengalihkan konteks direktori).\n    *   Mencegah attacker menargetkan symlink yang mengarah keluar dari struktur direktori yang diharapkan.\n\n2.  **`os.path.abspath` setelah Join**:\n    *   Menormalisasi `../` dan `./`. Jika input adalah `../../etc`, `abspath` akan menguranginya menjadi path absolut nyata.\n    *   Ini menetralisir upaya obfuscation path.\n\n3.  **Validasi `startswith` dengan Separator**:\n    *   Cek `normalized_path.startswith(self.base_directory + os.sep)` sangat krusial.\n    *   Tanpa separator, path `/var/www/uploads_evil/file.txt` akan lolos validasi jika base adalah `/var/www/uploads`.\n\n4.  **Logging Audit**:\n    *   Setiap upaya traversal yang diblokir dicatat. Ini wajib untuk monitoring insiden keamanan (SIEM).\n\n### Rekomendasi Tambahan dari ShiroAI\n\n*   **Chroot/Jail**: Untuk isolasi tingkat tinggi, pertimbangkan menjalankan proses dalam lingkungan terkurung (container/docker) dengan bind mount hanya pada direktori yang diperlukan.\n*   **Permission File**: Pastikan user yang menjalankan script Python memiliki permission `read/write` hanya pada direktori yang diizinkan, bukan root atau user sistem lain.\n*   **Validasi Ekstensi**: Selain path, validasi ekstensi file (whitelist) untuk mencegah upload shell script (`.php`, `.py`, `.sh`) jika ini digunakan untuk fitur upload.\n\nButuh implementasi untuk framework spesifik (Django/Flask) atau konfigurasi server (Nginx/Apache) untuk lapisan pertahanan tambahan? Beritahu saya.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python function to sanitize user input to prevent XSS attacks", "output": "Input sanitization saja tidak cukup untuk mencegah XSS secara menyeluruh. Prinsip utama keamanan web adalah **Output Escaping** sesuai konteks (HTML, JavaScript, URL, CSS). Namun, untuk validasi awal dan pembersihan data masuk, gunakan fungsi berikut.\n\nFungsi ini menggunakan `html.escape` dari standard library Python untuk mengencode karakter berbahaya menjadi HTML entities.\n\n```python\nimport html\nfrom typing import Union\n\ndef sanitize_input(user_input: Union[str, None]) -> str:\n    \"\"\"\n    Sanitasi input user untuk mencegah XSS dasar pada konteks HTML.\n    PERINGATAN: Ini bukan solusi tunggal. Wajib lakukan escaping saat rendering output.\n    \"\"\"\n    if user_input is None:\n        return \"\"\n    \n    if not isinstance(user_input, str):\n        try:\n            user_input = str(user_input)\n        except Exception:\n            return \"\"\n\n    # Encode karakter khusus HTML (<, >, &, \", ')\n    return html.escape(user_input, quote=True)\n\n# Contoh Penggunaan\nif __name__ == \"__main__\":\n    payload = '<script>alert(\"XSS\")</script>'\n    safe_output = sanitize_input(payload)\n    print(safe_output) \n    # Output: &lt;script&gt;alert(&quot;XSS&quot;)&lt;/script&gt;\n```\n\n### Catatan Teknis Keamanan:\n1.  **Konteks Output:** Fungsi di atas hanya aman untuk **HTML Body**. Jika input dimasukkan ke dalam atribut HTML, JavaScript, atau URL, Anda memerlukan encoder yang spesifik untuk konteks tersebut.\n2.  **Framework Security:** Jika menggunakan Flask, Django, atau FastAPI, manfaatkan templating engine mereka (Jinja2, Django Templates) yang sudah memiliki auto-escaping aktif. Jangan buat sendiri jika tidak terpaksa.\n3.  **Defense in Depth:** Implementasikan **Content Security Policy (CSP)** header di server Anda untuk membatasi sumber skript yang boleh dieksekusi.\n\n```nginx\n# Contoh konfigurasi CSP di Nginx\nadd_header Content-Security-Policy \"default-src 'self'; script-src 'self'; object-src 'none';\";\n```\n\nPastikan validasi dilakukan di server-side. Validasi client-side mudah dibypass.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python script to hash passwords using argon2", "output": "Gunakan library `argon2-cffi`. Ini standar industri saat ini untuk password hashing yang resisten terhadap GPU cracking. Jangan pakai MD5 atau SHA1 untuk password.\n\n### 1. Instalasi Dependency\n\n```bash\npip install argon2-cffi\n```\n\n### 2. Implementasi Script (Hash & Verify)\n\nSimpan sebagai `security_hash.py`. Script ini mencakup hashing dan verifikasi.\n\n```python\nfrom argon2 import PasswordHasher\nfrom argon2.exceptions import VerifyMismatchError\n\n# Inisialisasi hasher dengan parameter keamanan standar\n# time_cost: berapa iterasi hashing\n# memory_cost: berapa MB memori yang digunakan\n# parallelism: berapa thread parallel\nph = PasswordHasher(time_cost=2, memory_cost=65536, parallelism=1)\n\ndef hash_password(password: str) -> str:\n    \"\"\"Generate hash dari password plaintext.\"\"\"\n    try:\n        password_hash = ph.hash(password)\n        return password_hash\n    except Exception as e:\n        raise RuntimeError(f\"Gagal hashing: {str(e)}\")\n\ndef verify_password(password_hash: str, password_input: str) -> bool:\n    \"\"\"Verifikasi password input terhadap hash yang tersimpan.\"\"\"\n    try:\n        ph.verify(password_hash, password_input)\n        # Cek apakah hash perlu di-rehash karena parameter keamanan naik\n        if ph.check_needs_rehash(password_hash):\n            print(\"[INFO] Hash perlu di-update ke parameter yang lebih aman.\")\n        return True\n    except VerifyMismatchError:\n        return False\n\nif __name__ == \"__main__\":\n    raw_password = \"SecurePassword123!\"\n    \n    # Proses Hashing\n    hashed = hash_password(raw_password)\n    print(f\"Generated Hash: {hashed}\")\n    \n    # Proses Verifikasi (Benar)\n    is_valid = verify_password(hashed, raw_password)\n    print(f\"Verifikasi (Benar): {is_valid}\")\n    \n    # Proses Verifikasi (Salah)\n    is_invalid = verify_password(hashed, \"WrongPassword\")\n    print(f\"Verifikasi (Salah): {is_invalid}\")\n```\n\n### 3. Catatan Teknis & Keamanan\n\n1.  **Salt Otomatis:** `argon2-cffi` menangani salt secara otomatis dan menyertakannya dalam string hash. Jangan mencoba memisahkan atau mengelola salt manual.\n2.  **Parameter Tuning:** Sesuaikan `time_cost` dan `memory_cost` dengan kapasitas server Anda. Targetkan durasi hashing sekitar 0.5 - 1 detik per request untuk mencegah brute-force, tapi jangan sampai membebani server saat login massal.\n3.  **Storage:** Simpan string hash langsung ke database (tipe `VARCHAR` atau `TEXT`). Jangan pernah menyimpan password plaintext.\n4.  **Rehashing:** Gunakan `check_needs_rehash` seperti di contoh atas. Jika Anda menaikkan parameter keamanan di masa depan, user akan di-hash ulang secara otomatis saat login berikutnya.\n\nImplementasikan ini di backend aplikasi Anda. Pastikan transmisi password dari client ke server sudah terenkripsi via HTTPS/TLS.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python function to encrypt and decrypt data using AES-256-CBC", "output": "Berikut adalah implementasi AES-256-CBC menggunakan library `cryptography` standar industri. Jangan gunakan `pycrypto` atau `pycryptodome` untuk produksi jika ada alternatif yang lebih maintained seperti ini.\n\n### Prasyarat\nInstall library terlebih dahulu:\n```bash\npip install cryptography\n```\n\n### Implementasi Kode\n```python\nimport os\nimport base64\nfrom cryptography.hazmat.primitives.ciphers import Cipher, algorithms, modes\nfrom cryptography.hazmat.primitives import padding\nfrom cryptography.hazmat.backends import default_backend\n\nclass AESCipher:\n    def __init__(self, key: bytes):\n        \"\"\"\n        Inisialisasi dengan key 32-byte (256-bit).\n        Key HARUS disimpan securely (env vars, KMS), jangan hardcode.\n        \"\"\"\n        if len(key) != 32:\n            raise ValueError(\"Key harus 32 byte untuk AES-256.\")\n        self.key = key\n\n    def encrypt(self, plaintext: str) -> str:\n        \"\"\"Enkripsi data string menjadi base64 encoded ciphertext.\"\"\"\n        iv = os.urandom(16)  # IV harus random dan unik per enkripsi\n        cipher = Cipher(algorithms.AES(self.key), modes.CBC(iv), backend=default_backend())\n        encryptor = cipher.encryptor()\n\n        # Padding data agar sesuai block size (128 bit / 16 byte)\n        padder = padding.PKCS7(128).padder()\n        padded_data = padder.update(plaintext.encode('utf-8')) + padder.finalize()\n\n        ciphertext = encryptor.update(padded_data) + encryptor.finalize()\n        \n        # Gabungkan IV + Ciphertext agar bisa digunakan saat decrypt\n        return base64.b64encode(iv + ciphertext).decode('utf-8')\n\n    def decrypt(self, token: str) -> str:\n        \"\"\"Dekripsi base64 token kembali ke string asli.\"\"\"\n        data = base64.b64decode(token)\n        iv = data[:16]\n        ciphertext = data[16:]\n\n        cipher = Cipher(algorithms.AES(self.key), modes.CBC(iv), backend=default_backend())\n        decryptor = cipher.decryptor()\n\n        padded_data = decryptor.update(ciphertext) + decryptor.finalize()\n\n        # Unpadding\n        unpadder = padding.PKCS7(128).unpadder()\n        plaintext = unpadder.update(padded_data) + unpadder.finalize()\n\n        return plaintext.decode('utf-8')\n\n# Contoh Penggunaan\nif __name__ == \"__main__\":\n    # GENERATE KEY SECARA AMAN DI PRODUCTION (JANGAN LIKE INI)\n    # key = os.urandom(32) \n    key = b\"0123456789abcdef0123456789abcdef\" # 32 bytes contoh\n    \n    aes = AESCipher(key)\n    secret = \"Data Sensitif PT Sherif Network\"\n    \n    encrypted = aes.encrypt(secret)\n    print(f\"Ciphertext: {encrypted}\")\n    \n    decrypted = aes.decrypt(encrypted)\n    print(f\"Plaintext: {decrypted}\")\n```\n\n### Catatan Keamanan Kritis (Wajib Baca)\n\n1.  **Integritas Data:** AES-CBC **tidak** menyediakan autentikasi. Ciphertext bisa dimanipulasi (padding oracle attack). Untuk produksi, wajib implementasikan **Encrypt-then-MAC** (gunakan HMAC-SHA256 pada ciphertext) atau ganti ke **AES-GCM** yang sudah include autentikasi.\n2.  **IV Management:** IV tidak perlu rahasia, tapi **wajib unik** untuk setiap enkripsi dengan key yang sama. Jangan pernah reuse IV.\n3.  **Key Storage:** Jangan pernah simpan key di dalam kode source. Gunakan Environment Variables atau solusi manajemen rahasia seperti HashiCorp Vault / AWS KMS.\n4.  **Rekomendasi:** Jika Anda membangun sistem baru, gunakan **AES-GCM**. CBC dianggap legacy untuk aplikasi modern yang membutuhkan kerahasiaan dan integritas sekaligus.\n\nJika butuh implementasi AES-GCM atau konfigurasi server untuk mengamankan key ini, beri tahu saya.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Implement a secure session management system using JWT", "output": "JWT bukan mekanisme session tradisional yang stateful. Jika Anda mengimplementasikannya sebagai pengganti session, Anda wajib mengikuti standar keamanan ketat untuk menghindari pencurian token (XSS) dan pemalsuan request (CSRF).\n\nBerikut adalah standar implementasi secure JWT session management sesuai protokol keamanan PT Sherif Network.\n\n### 1. Arsitektur Token (Access & Refresh)\nJangan gunakan satu token untuk semua keperluan. Gunakan pola **Dual Token**:\n*   **Access Token:** Umur pendek (5-15 menit). Digunakan untuk akses API.\n*   **Refresh Token:** Umur lebih panjang (7-30 hari). Digunakan hanya untuk mendapatkan Access Token baru. Disimpan di database untuk validitas revokasi.\n\n### 2. Implementasi Kode (Python - PyJWT)\nContoh backend secure menggunakan `HttpOnly` cookies. **Jangan pernah menyimpan JWT di LocalStorage.**\n\n```python\nimport jwt\nimport datetime\nimport os\nfrom flask import Flask, request, jsonify, make_response\n\napp = Flask(__name__)\n\n# KONFIGURASI WAJIB\nJWT_SECRET = os.environ.get(\"JWT_SECRET\")  # Jangan hardcode!\nJWT_ALGORITHM = \"HS256\"  # Gunakan RS256 untuk skala besar\nTOKEN_EXPIRY = 15  # Menit\nREFRESH_EXPIRY = 7  # Hari\n\ndef generate_tokens(user_id):\n    now = datetime.datetime.utcnow()\n    \n    # Access Token\n    access_payload = {\n        \"sub\": user_id,\n        \"exp\": now + datetime.timedelta(minutes=TOKEN_EXPIRY),\n        \"iat\": now,\n        \"type\": \"access\"\n    }\n    access_token = jwt.encode(access_payload, JWT_SECRET, algorithm=JWT_ALGORITHM)\n\n    # Refresh Token\n    refresh_payload = {\n        \"sub\": user_id,\n        \"exp\": now + datetime.timedelta(days=REFRESH_EXPIRY),\n        \"iat\": now,\n        \"type\": \"refresh\"\n    }\n    refresh_token = jwt.encode(refresh_payload, JWT_SECRET, algorithm=JWT_ALGORITHM)\n    \n    return access_token, refresh_token\n\n@app.route('/login', methods=['POST'])\ndef login():\n    # Validasi kredensial user di sini...\n    user_id = \"user_123\" \n    \n    access_token, refresh_token = generate_tokens(user_id)\n    \n    # Simpan Refresh Token ID ke database untuk revokasi nanti\n    # db.save_refresh_token(user_id, refresh_token)\n\n    response = make_response(jsonify({\"message\": \"Login successful\"}))\n    \n    # SET COOKIE DENGAN FLAG KEAMANAN\n    response.set_cookie(\n        'access_token',\n        access_token,\n        httponly=True,      # Mencegah akses via JavaScript (XSS)\n        secure=True,        # Hanya kirim via HTTPS\n        samesite='Strict',  # Mencegah CSRF\n        max_age=TOKEN_EXPIRY * 60\n    )\n    \n    response.set_cookie(\n        'refresh_token',\n        refresh_token,\n        httponly=True,\n        secure=True,\n        samesite='Strict',\n        max_age=REFRESH_EXPIRY * 24 * 60 * 60\n    )\n    \n    return response\n\n@app.route('/verify', methods=['POST'])\ndef verify():\n    token = request.cookies.get('access_token')\n    \n    if not token:\n        return jsonify({\"error\": \"Token missing\"}), 401\n\n    try:\n        # Verifikasi Signature dan Expiry\n        data = jwt.decode(token, JWT_SECRET, algorithms=[JWT_ALGORITHM])\n        return jsonify({\"valid\": True, \"user\": data['sub']})\n    except jwt.ExpiredSignatureError:\n        return jsonify({\"error\": \"Token expired\"}), 401\n    except jwt.InvalidTokenError:\n        return jsonify({\"error\": \"Invalid token\"}), 401\n```\n\n### 3. Hardening & Mitigasi Risiko\n\n#### A. Penyimpanan Token\n*   **Wajib:** `HttpOnly`, `Secure`, `SameSite=Strict`.\n*   **Dilarang:** Menyimpan token sensitif di `LocalStorage` atau `SessionStorage`. Ini rentan terhadap XSS. Jika attacker bisa injeksi JS, mereka bisa mencuri token.\n\n#### B. Algoritma Signing\n*   Gunakan **RS256** (Asymmetric) jika Anda memiliki banyak layanan mikro. Private key untuk signing, Public key untuk verifikasi.\n*   Jika menggunakan **HS256** (Symmetric), pastikan secret key minimal 32 karakter acak kompleks dan disimpan di Environment Variables, bukan di kode.\n*   **Validasi Algoritma:** Pastikan library JWT Anda memvalidasi algoritma. Jangan izinkan attacker mengubah header menjadi `none` atau `HS256` saat server mengharapkan `RS256`.\n\n#### C. Revokasi Session (Logout)\nKarena JWT stateless, Anda tidak bisa \"menghapus\" token secara langsung sebelum expiry.\n*   **Solusi:** Gunakan **Denylist/Blacklist** di Redis untuk Access Token yang di-logout sebelum expiry.\n*   **Refresh Token:** Simpan JTI (JWT ID) di database. Saat logout, hapus atau tandai JTI tersebut sebagai invalid. Saat refresh request masuk, cek database terlebih dahulu.\n\n#### D. Header Keamanan\nTambahkan header ini di semua response server:\n```http\nStrict-Transport-Security: max-age=31536000; includeSubDomains\nX-Content-Type-Options: nosniff\nX-Frame-Options: DENY\nContent-Security-Policy: default-src 'self'\n```\n\n### 4. Peringatan Keamanan\n1.  **Jangan masukkan data sensitif** (password, PII) ke dalam payload JWT. Payload JWT hanya di-encode (Base64), bukan di-enkripsi. Siapa saja bisa membacanya.\n2.  **Rotasi Kunci:** Jika menggunakan HS256, rencanakan rotasi secret key secara berkala.\n3.  **Rate Limiting:** Endpoint refresh token wajib dilindungi rate limiting untuk mencegah brute-force.\n\nImplementasi ini adalah standar minimum. Jika Anda menemukan celah pada implementasi ini, segera lakukan audit kode dan patch. Keamanan adalah proses, bukan fitur.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python script to automate backup rotation based on retention policy", "output": "[Cloud error: The read operation timed out]", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a script to recursively find all files with specific permissions in a directory", "output": "Gunakan `find`. Ini alat native Unix/Linux paling efisien untuk operasi ini. Jangan buat loop recursif manual menggunakan Python atau PHP jika Anda memiliki akses shell; itu tidak optimal dan membuang resource server.\n\nBerikut adalah Bash script untuk audit permissions secara cepat.\n\n### Script: `perm_audit.sh`\n\n```bash\n#!/bin/bash\n# ShiroAI Security Tool - PT Sherif Network\n# Usage: ./perm_audit.sh [directory] [permission_octal]\n\nTARGET_DIR=\"${1:-.}\"\nPERM_MASK=\"${2:-777}\"\n\nif [ ! -d \"$TARGET_DIR\" ]; then\n    echo \"Error: Directory '$TARGET_DIR' tidak ditemukan.\"\n    exit 1\nfi\n\necho \"[*] Memindai $TARGET_DIR untuk permission $PERM_MASK...\"\nfind \"$TARGET_DIR\" -type f -perm \"$PERM_MASK\" -ls\n```\n\n### Cara Penggunaan\n1.  Simpan script, lalu berikan eksekusi:\n    ```bash\n    chmod +x perm_audit.sh\n    ```\n2.  Jalankan audit:\n    ```bash\n    ./perm_audit.sh /var/www/html 777\n    ```\n\n### Checklist Keamanan Kritikal (Wajib Audit)\nSebagai administrator server, Anda harus rutin memeriksa celah berikut. Jangan hanya mencari satu permission spesifik.\n\n**1. Cari File World-Writable (Bahaya Tinggi)**\nFile yang bisa dimodifikasi oleh user mana saja di sistem.\n```bash\nfind /path/to/audit -type f -perm -002\n```\n\n**2. Cari File SUID/SGID (Potensi Privilege Escalation)**\nFile yang berjalan dengan permission owner (root) meskipun dieksekusi user biasa.\n```bash\n# SUID\nfind /path/to/audit -type f -perm -4000\n# SGID\nfind /path/to/audit -type f -perm -2000\n```\n\n**3. Cari File Konfigurasi yang Terbaca Publik**\nPastikan file `.env` atau `config.php` tidak readable oleh world.\n```bash\nfind /path/to/audit -type f -name \".env\" -perm /044\n```\n\n### Mitigasi\nJika menemukan file mencurigakan:\n1.  **Revokasi Permission:**\n    ```bash\n    chmod 644 nama_file\n    ```\n2.  **Investigasi:** Cek log audit (`ausearch` atau `/var/log/auth.log`) untuk melihat siapa yang mengubah permission tersebut.\n3.  **Isolasi:** Jika file terlihat mencurigakan (webshell), jangan dihapus dulu. Amankan untuk forensik, lalu hapus.\n\nJalankan audit ini secara berkala. Keamanan server bukan sekali setup, tapi proses berkelanjutan.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python function to sanitize user input against XSS attacks by stripping dangerous HTML tags", "output": "Jangan pernah gunakan regex atau string manipulation manual untuk sanitasi HTML. Itu mudah di-bypass (contoh: `<scr<script>ipt>`). Sebagai ahli keamanan, saya mewajibkan penggunaan library yang sudah teraudit secara standar industri.\n\nGunakan library `bleach` untuk parsing HTML yang aman berdasarkan whitelist.\n\n### Implementasi Sanitasi XSS (Python)\n\nInstall dependency terlebih dahulu:\n```bash\npip install bleach\n```\n\nBerikut fungsi sanitasi yang aman:\n\n```python\nimport bleach\n\ndef sanitize_input(user_input: str) -> str:\n    \"\"\"\n    Sanitasi input user untuk mencegah XSS dengan whitelist tag aman.\n    Hanya tag yang diizinkan yang akan lolos, sisanya di-escape atau dibuang.\n    \"\"\"\n    if not isinstance(user_input, str):\n        raise TypeError(\"Input harus berupa string\")\n\n    # Whitelist tag yang aman (sesuaikan kebutuhan aplikasi)\n    allowed_tags = [\n        'p', 'br', 'strong', 'em', 'u', 'h1', 'h2', 'h3', \n        'a', 'ul', 'ol', 'li', 'code', 'pre'\n    ]\n\n    # Whitelist atribut aman (hati-hati dengan on* event handlers)\n    allowed_attributes = {\n        'a': ['href', 'title', 'rel'],\n        '*': ['class', 'id']\n    }\n\n    # Protokol URL yang aman untuk link\n    allowed_protocols = ['http', 'https', 'mailto']\n\n    # Proses pembersihan\n    clean_html = bleach.clean(\n        user_input,\n        tags=allowed_tags,\n        attributes=allowed_attributes,\n        protocols=allowed_protocols,\n        strip=True  # Hapus tag yang tidak diizinkan, bukan hanya escape\n    )\n\n    return clean_html\n\n# Contoh Penggunaan\nif __name__ == \"__main__\":\n    malicious_input = \"<script>alert('XSS')</script><p>Safe content</p><img src=x onerror=alert(1)>\"\n    safe_output = sanitize_input(malicious_input)\n    print(safe_output) \n    # Output: <p>Safe content</p> (Script dan img berbahaya hilang)\n```\n\n### Catatan Keamanan Wajib:\n\n1.  **Defense in Depth:** Sanitasi input saja tidak cukup. Selalu lakukan **Output Encoding** sesuai konteks (HTML, JS, CSS, URL) saat merender data ke browser.\n2.  **Content Security Policy (CSP):** Implementasikan header CSP ketat di server Anda untuk membatasi sumber eksekusi skrip.\n3.  **HttpOnly Cookies:** Pastikan session cookie menggunakan flag `HttpOnly` dan `Secure` untuk mencegah pencurian session via XSS.\n4.  **Validasi Tipe:** Pastikan input sesuai tipe data yang diharapkan sebelum diproses lebih lanjut.\n\nKode di atas adalah standar minimum. Jika aplikasi Anda menangani input rich text yang kompleks, pertimbangkan menggunakan sanitizer berbasis DOM parsing yang lebih ketat.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python script to find and delete duplicate files based on hash", "output": "PERINGATAN: Skrip ini melakukan operasi penghapusan file. Kesalahan konfigurasi dapat menyebabkan kehilangan data permanen. Selalu lakukan backup sebelum eksekusi.\n\nBerikut adalah skrip Python optimasi tinggi untuk mendeteksi dan menghapus duplikasi file menggunakan hashing SHA-256. Skrip ini mengimplementasikan checksizedahulu untuk efisiensi, lalu hashing hanya pada file dengan ukuran sama.\n\n### Fitur Keamanan & Optimasi\n1.  **SHA-256:** Menggunakan algoritma hash kriptografis yang kuat (bukan MD5).\n2.  **Size Pre-Check:** Menghindari hashing unnecessary dengan membandingkan ukuran file terlebih dahulu.\n3.  **Dry-Run Mode:** Default mode adalah aman (hanya laporan). Penghapusan fisik memerlukan flag eksplisit.\n4.  **Error Handling:** Menangani exception pada file yang terkunci atau tanpa izin akses.\n\n### Skrip: `dedup.py`\n\n```python\nimport os\nimport sys\nimport hashlib\nimport argparse\nfrom collections import defaultdict\n\ndef calculate_hash(filepath, block_size=65536):\n    \"\"\"Menghitung SHA-256 hash dari file secara chunked untuk efisiensi memori.\"\"\"\n    hasher = hashlib.sha256()\n    try:\n        with open(filepath, 'rb') as f:\n            for block in iter(lambda: f.read(block_size), b''):\n                hasher.update(block)\n        return hasher.hexdigest()\n    except (PermissionError, OSError) as e:\n        print(f\"[ERROR] Tidak dapat membaca {filepath}: {e}\")\n        return None\n\ndef find_duplicates(path):\n    \"\"\"Menemukan file duplikat berdasarkan ukuran dan hash.\"\"\"\n    size_map = defaultdict(list)\n    \n    print(f\"[*] Memindai direktori: {os.path.abspath(path)}\")\n    \n    # Tahap 1: Grouping berdasarkan ukuran file (Optimasi)\n    for root, _, files in os.walk(path):\n        for filename in files:\n            filepath = os.path.join(root, filename)\n            # Skip symlinks untuk mencegah loop\n            if os.path.islink(filepath):\n                continue\n            try:\n                size = os.path.getsize(filepath)\n                if size == 0:\n                    continue # Skip file kosong\n                size_map[size].append(filepath)\n            except OSError:\n                continue\n\n    # Tahap 2: Hashing hanya pada file dengan ukuran sama\n    hash_map = defaultdict(list)\n    for size, files in size_map.items():\n        if len(files) > 1:\n            for filepath in files:\n                file_hash = calculate_hash(filepath)\n                if file_hash:\n                    hash_map[file_hash].append(filepath)\n\n    # Filter hanya yang duplikat\n    duplicates = {h: files for h, files in hash_map.items() if len(files) > 1}\n    return duplicates\n\ndef main():\n    parser = argparse.ArgumentParser(description=\"ShiroAI Duplicate File Cleaner\")\n    parser.add_argument(\"path\", help=\"Path direktori target\")\n    parser.add_argument(\"--execute\", action=\"store_true\", help=\"Eksekusi penghapusan (Default: Dry-Run)\")\n    parser.add_argument(\"--keep\", type=int, default=1, help=\"Jumlah file asli yang disimpan per grup hash\")\n    \n    args = parser.parse_args()\n\n    if not os.path.isdir(args.path):\n        print(\"[ERROR] Path tidak valid atau bukan direktori.\")\n        sys.exit(1)\n\n    duplicates = find_duplicates(args.path)\n\n    if not duplicates:\n        print(\"[OK] Tidak ada file duplikat ditemukan.\")\n        return\n\n    total_recoverable = 0\n    files_to_delete = []\n\n    print(f\"\\n[!] Ditemukan {len(duplicates)} grup file duplikat.\")\n    \n    for file_hash, files in duplicates.items():\n        # Urutkan agar penghapusan deterministik (misal: berdasarkan path terpendek)\n        files.sort()\n        keep_files = files[:args.keep]\n        delete_files = files[args.keep:]\n        \n        for f in delete_files:\n            try:\n                size = os.path.getsize(f)\n                total_recoverable += size\n                files_to_delete.append(f)\n            except OSError:\n                continue\n\n    print(f\"[INFO] Total ruang yang dapat dikembalikan: {total_recoverable / (1024 * 1024):.2f} MB\")\n    print(f\"[INFO] Total file kandidat hapus: {len(files_to_delete)}\")\n\n    if not args.execute:\n        print(\"\\n[DRY-RUN] Mode aman aktif. Tidak ada file yang dihapus.\")\n        print(\"Gunakan flag --execute untuk menghapus file secara permanen.\")\n        print(\"\\nDaftar file yang akan dihapus:\")\n        for f in files_to_delete:\n            print(f\"  - {f}\")\n        return\n\n    # Eksekusi Penghapusan\n    confirm = input(\"\\n[PERINGATAN] Anda akan menghapus file secara permanen. Ketik 'YES' untuk lanjut: \")\n    if confirm != 'YES':\n        print(\"[ABORT] Operasi dibatalkan.\")\n        return\n\n    deleted_count = 0\n    for filepath in files_to_delete:\n        try:\n            os.remove(filepath)\n            print(f\"[DELETE] {filepath}\")\n            deleted_count += 1\n        except Exception as e:\n            print(f\"[ERROR] Gagal menghapus {filepath}: {e}\")\n\n    print(f\"\\n[SELESAI] {deleted_count} file berhasil dihapus.\")\n\nif __name__ == \"__main__\":\n    main()\n```\n\n### Cara Penggunaan\n\n1.  **Scan Awal (Aman):**\n    Jalankan tanpa flag `--execute` untuk melihat laporan saja.\n    ```bash\n    python dedup.py /path/to/target-directory\n    ```\n\n2.  **Eksekusi Penghapusan:**\n    Hanya gunakan ini setelah Anda memverifikasi output dari scan awal.\n    ```bash\n    python dedup.py /path/to/target-directory --execute\n    ```\n\n3.  **Menyimpan Lebih dari 1 File Asli:**\n    Jika Anda ingin menyimpan 2 salinan untuk redundansi:\n    ```bash\n    python dedup.py /path/to/target-directory --execute --keep 2\n    ```\n\n### Catatan Teknis\n*   **Integritas Data:** SHA-256 memiliki probabilitas kolisi yang sangat rendah, namun tidak mustahil. Untuk data kritis, verifikasi manual tetap disarankan.\n*   **Izin Akses:** Pastikan skrip dijalankan dengan user yang memiliki izin baca/tulis pada direktori target. Hindari menjalankan sebagai `root` kecuali diperlukan untuk menghindari penghapusan sistem yang tidak sengaja.\n*   **Backup:** Selalu pastikan Anda memiliki backup eksternal sebelum menjalankan skrip manipulasi file massal.\n\nJika ada pertanyaan mengenai implementasi pada server produksi atau integrasi dengan panel manajemen (Pterodactyl/cPanel), tanyakan langsung.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python script to monitor CPU and memory usage and log alerts", "output": "Berikut adalah script monitoring sistem berbasis Python menggunakan library `psutil`. Script ini dirancang untuk efisiensi, memiliki mekanisme log rotation untuk mencegah disk full, dan mencatat alert jika threshold terlampaui.\n\n### Prasyarat\nInstall dependency wajib:\n```bash\npip install psutil\n```\n\n### Script Monitoring (`monitor.py`)\n\n```python\nimport psutil\nimport logging\nimport time\nfrom logging.handlers import RotatingFileHandler\nfrom datetime import datetime\n\n# KONFIGURASI THRESHOLD\nCPU_THRESHOLD = 80.0  # Persen\nMEM_THRESHOLD = 80.0  # Persen\nCHECK_INTERVAL = 5    # Detik\n\n# KONFIGURASI LOGGING\nLOG_FILE = '/var/log/shiro_monitor.log'\nMAX_BYTES = 10485760  # 10MB\nBACKUP_COUNT = 3      # Simpan 3 file log lama\n\ndef setup_logging():\n    logger = logging.getLogger('ShiroMonitor')\n    logger.setLevel(logging.INFO)\n    \n    # Gunakan RotatingFileHandler untuk keamanan disk\n    handler = RotatingFileHandler(LOG_FILE, maxBytes=MAX_BYTES, backupCount=BACKUP_COUNT)\n    formatter = logging.Formatter('%(asctime)s - %(levelname)s - %(message)s')\n    handler.setFormatter(formatter)\n    \n    logger.addHandler(handler)\n    return logger\n\ndef check_system_health(logger):\n    try:\n        cpu_usage = psutil.cpu_percent(interval=1)\n        memory = psutil.virtual_memory()\n        mem_usage = memory.percent\n        \n        status = \"OK\"\n        \n        if cpu_usage > CPU_THRESHOLD:\n            logger.warning(f\"ALERT: CPU Usage High - {cpu_usage}% (Threshold: {CPU_THRESHOLD}%)\")\n            status = \"CRITICAL\"\n            \n        if mem_usage > MEM_THRESHOLD:\n            logger.warning(f\"ALERT: Memory Usage High - {mem_usage}% (Threshold: {MEM_THRESHOLD}%)\")\n            status = \"CRITICAL\"\n            \n        if status == \"OK\":\n            logger.info(f\"System Healthy - CPU: {cpu_usage}% | MEM: {mem_usage}%\")\n            \n    except Exception as e:\n        logger.error(f\"Monitoring Error: {str(e)}\")\n\nif __name__ == \"__main__\":\n    logger = setup_logging()\n    logger.info(\"=== ShiroMonitor Started ===\")\n    \n    try:\n        while True:\n            check_system_health(logger)\n            time.sleep(CHECK_INTERVAL)\n    except KeyboardInterrupt:\n        logger.info(\"=== ShiroMonitor Stopped by User ===\")\n```\n\n### Instruksi Deployment & Keamanan\n\n1.  **Izin File Log:**\n    Pastikan file log tidak bisa dibaca/ditulis oleh user sembarangan.\n    ```bash\n    sudo touch /var/log/shiro_monitor.log\n    sudo chown root:root /var/log/shiro_monitor.log\n    sudo chmod 600 /var/log/shiro_monitor.log\n    ```\n\n2.  **Jalankan sebagai Service (Systemd):**\n    Jangan jalankan manual di background. Buat unit file systemd (`/etc/systemd/system/shiro-monitor.service`) agar restart otomatis jika crash.\n    ```ini\n    [Unit]\n    Description=ShiroAI System Monitor\n    After=network.target\n\n    [Service]\n    User=root\n    ExecStart=/usr/bin/python3 /opt/shiro/monitor.py\n    Restart=always\n\n    [Install]\n    WantedBy=multi-user.target\n    ```\n\n3.  **Keamanan:**\n    *   Script ini harus dijalankan dengan privilege minimal yang diperlukan. Jika hanya butuh read access ke `/proc`, hindari running sebagai `root` jika tidak\u0e08\u0e33\u0e40\u0e1b\u0e47\u0e19.\n    *   Pastikan path log (`/var/log/`) aman dari write access user non-privilege untuk mencegah log poisoning.\n\nGunakan script ini untuk baseline monitoring. Untuk produksi, integrasikan dengan webhook (Slack/Discord/Telegram) pada bagian `logger.warning` untuk notifikasi real-time.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python script to scan open ports on a target host using sockets", "output": "\u26a0\ufe0f **PERINGATAN KERAS**\nGunakan script ini **HANYA** pada infrastruktur yang Anda miliki sendiri atau memiliki izin tertulis eksplisit untuk melakukan audit keamanan. Scanning tanpa otorisasi adalah tindakan ilegal dan dapat dikategorikan sebagai serangan siber. PT Sherif Network tidak bertanggung jawab atas penyalahgunaan kode ini.\n\nBerikut adalah script Python dasar untuk audit port menggunakan `socket` (TCP Connect Scan).\n\n### Script Audit Port (Python)\n\n```python\nimport socket\nimport sys\nfrom datetime import datetime\n\ndef scan_port(host, port):\n    try:\n        sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)\n        socket.setdefaulttimeout(1)\n        result = sock.connect_ex((host, port))\n        if result == 0:\n            return True\n        sock.close()\n    except Exception as e:\n        pass\n    return False\n\ndef main():\n    if len(sys.argv) != 2:\n        print(\"Usage: python port_scan.py <target_host>\")\n        sys.exit(1)\n\n    target = sys.argv[1]\n    \n    # Validasi IP/Hostname\n    try:\n        ip = socket.gethostbyname(target)\n    except socket.gaierror:\n        print(\"Hostname tidak dapat diselesaikan.\")\n        sys.exit(1)\n\n    print(f\"[*] Memulai audit pada: {ip}\")\n    print(f\"[*] Waktu: {datetime.now()}\")\n    \n    open_ports = []\n    \n    # Scan port umum (1-1024)\n    for port in range(1, 1025):\n        if scan_port(ip, port):\n            print(f\"[+] Port {port}: OPEN\")\n            open_ports.append(port)\n        # else:\n        #     print(f\"[-] Port {port}: CLOSED\") # Uncomment untuk debug\n            \n    print(f\"\\n[*] Selesai. Total port terbuka: {len(open_ports)}\")\n\nif __name__ == \"__main__\":\n    main()\n```\n\n### Analisis Teknis & Mitigasi\n\nSebagai ahli keamanan, mengetahui port terbuka hanyalah langkah awal. Langkah selanjutnya adalah **pengerasan (hardening)**.\n\n1.  **Minimalkan Surface Attack:**\n    Tutup semua port yang tidak digunakan. Hanya layanan esensial (misal: 80, 443, 22) yang boleh terbuka.\n    ```bash\n    # Contoh menutup port menggunakan UFW\n    sudo ufw deny 23/tcp\n    sudo ufw deny 3306/tcp\n    ```\n\n2.  **Gunakan Firewall Konfigurasi Ketat:**\n    Batasi akses SSH hanya dari IP tertentu.\n    ```bash\n    sudo ufw allow from 192.168.1.50 to any port 22\n    ```\n\n3.  **Deteksi Scanning:**\n    Instal tools seperti `fail2ban` atau konfigurasi IDS (Intrusion Detection System) untuk memblokir IP yang melakukan scanning agresif.\n    ```bash\n    sudo apt install fail2ban\n    sudo systemctl enable fail2ban\n    ```\n\n4.  **Audit Berkala:**\n    Jalankan script di atas secara rutin pada server Anda sendiri untuk memastikan tidak ada layanan baru yang terbuka secara tidak sengaja setelah update atau instalasi software.\n\nLakukan audit keamanan secara bertanggung jawab.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Create a Python script to audit file permissions in a specific directory recursively", "output": "Berikut adalah skrip Python untuk audit izin akses file secara rekursif. Skrip ini dirancang untuk mengidentifikasi konfigurasi permissions yang berisiko tinggi seperti *world-writable*, *SUID/SGID bits*, dan file sensitif yang terekspos ke publik.\n\nGunakan skrip ini untuk hardening server atau audit kepatuhan keamanan.\n\n### `permission_audit.py`\n\n```python\n#!/usr/bin/env python3\nimport os\nimport stat\nimport sys\nimport argparse\nfrom pathlib import Path\n\nclass PermissionAuditor:\n    def __init__(self, root_path):\n        self.root_path = Path(root_path).resolve()\n        self.risks = []\n\n    def get_permission_string(self, mode):\n        \"\"\"Konversi mode stat ke string readable (rwxrwxrwx)\"\"\"\n        perms = \"\"\n        for who in [\"USR\", \"GRP\", \"OTH\"]:\n            for what in [\"R\", \"W\", \"X\"]:\n                flag = getattr(stat, f\"S_I{what}{who}\")\n                perms += what.lower() if mode & flag else \"-\"\n        \n        # Check SUID/SGID/Sticky\n        if mode & stat.S_ISUID: perms[3] = 's' if perms[3] == 'x' else 'S'\n        if mode & stat.S_ISGID: perms[6] = 's' if perms[6] == 'x' else 'S'\n        if mode & stat.S_ISVTX: perms[9] = 't' if perms[9] == 'x' else 'T'\n        \n        return perms\n\n    def analyze_risks(self, file_path, mode, is_dir):\n        \"\"\"Identifikasi potensi kerentanan keamanan\"\"\"\n        risks = []\n        octal_perms = oct(stat.S_IMODE(mode))\n\n        # 1. World Writable (Opsi paling kritis)\n        if mode & stat.S_IWOTH:\n            risks.append(f\"[KRITIS] World-Writable ({octal_perms})\")\n\n        # 2. SUID/SGID pada file executable\n        if not is_dir:\n            if mode & stat.S_ISUID:\n                risks.append(f\"[TINGGI] SUID Bit Set ({octal_perms})\")\n            if mode & stat.S_ISGID:\n                risks.append(f\"[TINGGI] SGID Bit Set ({octal_perms})\")\n\n        # 3. Directory World Writable tanpa Sticky Bit\n        if is_dir and (mode & stat.S_IWOTH) and not (mode & stat.S_ISVTX):\n            risks.append(f\"[KRITIS] Dir World-Writable tanpa Sticky Bit\")\n\n        # 4. File sensitif terbaca publik (heuristik sederhana)\n        sensitive_keywords = ['.env', 'config', 'password', 'secret', 'key', '.ssh']\n        if any(k in file_path.name.lower() for k in sensitive_keywords):\n            if mode & stat.S_IROTH:\n                risks.append(f\"[SEDANG] File Sensitif Readable by Others\")\n\n        return risks\n\n    def audit(self):\n        print(f\"[*] Memulai audit pada: {self.root_path}\")\n        print(f\"{'FILE PATH':<50} | {'PERMS':<10} | {'RISK'}\")\n        print(\"-\" * 80)\n\n        try:\n            for path in self.root_path.rglob('*'):\n                try:\n                    file_stat = path.lstat() # lstat untuk tidak mengikuti symlink\n                    mode = file_stat.st_mode\n                    is_dir = stat.S_ISDIR(mode)\n                    \n                    risks = self.analyze_risks(path, mode, is_dir)\n                    perm_str = self.get_permission_string(mode)\n\n                    if risks:\n                        risk_msg = \", \".join(risks)\n                        print(f\"{str(path):<50} | {perm_str:<10} | {risk_msg}\")\n                        self.risks.append((path, risks))\n                    \n                except PermissionError:\n                    print(f\"{str(path):<50} | {'ACCESS DENIED':<10} | [INFO] Skipping\")\n                except Exception as e:\n                    print(f\"{str(path):<50} | {'ERROR':<10} | {str(e)}\")\n\n        except KeyboardInterrupt:\n            print(\"\\n[!] Audit dihentikan pengguna.\")\n            sys.exit(1)\n\n        print(\"-\" * 80)\n        print(f\"[+] Audit Selesai. Total risiko ditemukan: {len(self.risks)}\")\n\nif __name__ == \"__main__\":\n    parser = argparse.ArgumentParser(description=\"ShiroAI Security Permission Auditor\")\n    parser.add_argument(\"directory\", help=\"Direktori target untuk audit\")\n    args = parser.parse_args()\n\n    if not os.path.isdir(args.directory):\n        print(f\"[!] Error: {args.directory} bukan direktori valid.\")\n        sys.exit(1)\n\n    auditor = PermissionAuditor(args.directory)\n    auditor.audit()\n```\n\n### Cara Penggunaan\n\n1.  **Simpan skrip** sebagai `permission_audit.py`.\n2.  **Jalankan** dengan Python 3:\n    ```bash\n    python3 permission_audit.py /path/to/target_directory\n    ```\n3.  **Privilege:** Jalankan sebagai user biasa untuk audit standar. Gunakan `sudo` jika perlu mengakses file sistem yang dibatasi, namun pastikan Anda memahami implikasi keamanan menjalankan skrip pihak ketiga dengan root.\n\n### Fokus Analisis Keamanan\n\nSkrip ini menyoroti hal berikut berdasarkan prinsip *Least Privilege*:\n\n1.  **World-Writable (`o+w`):** Izin paling berbahaya. Memungkinkan user mana pun di sistem memodifikasi atau menghapus file.\n2.  **SUID/SGID Bits:** Memungkinkan file dieksekusi dengan izin pemilik/file. Sering disalahgunakan untuk eskalasi privilege jika file tersebut rentan.\n3.  **Directory tanpa Sticky Bit:** Jika direktori world-writable tanpa sticky bit (`t`), user lain dapat menghapus file milik user lain di dalamnya.\n4.  **Eksposur File Sensitif:** Mendeteksi file bernama konfigurasi (`.env`, `config`) yang dapat dibaca oleh user lain (`o+r`).\n\n### Rekomendasi Mitigasi\n\nJika skrip menemukan risiko, segera perbaiki izin aksesnya:\n\n```bash\n# Hapus izin write untuk others\nchmod o-w /path/to/file\n\n# Hapus SUID/SGID jika tidak diperlukan\nchmod u-s,g-s /path/to/file\n\n# Set sticky bit pada directory shared\nchmod +t /path/to/directory\n\n# Pastikan file sensitif hanya bisa dibaca owner\nchmod 600 /path/to/.env\n```\n\nLakukan audit ini secara berkala sebagai bagian dari prosedur maintenance server PT Sherif Network.", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
{"instruction": "Write a Python script to parse Apache access logs for suspicious IP addresses", "output": "Ini adalah skrip Python untuk audit defensif log Apache. Skrip ini mendeteksi anomali berdasarkan frekuensi request, kode status HTTP, dan pola serangan umum (SQLi, XSS, LFI).\n\nGunakan untuk monitoring server produksi. Pastikan Anda memiliki izin akses terhadap log tersebut.\n\n### Script: `apache_security_audit.py`\n\n```python\nimport re\nimport sys\nfrom collections import defaultdict\nfrom datetime import datetime\n\n# Konfigurasi Threshold\nTHRESHOLD_REQUESTS = 100      # Request per IP dalam satu file log\nTHRESHOLD_ERRORS = 10         # Error 4xx/5xx per IP\nLOG_FILE = \"access.log\"       # Ganti dengan path log Anda\n\n# Pattern Serangan Umum (Defensive Signatures)\nATTACK_PATTERNS = [\n    r\"(?i)(union\\s+select)\",          # SQL Injection\n    r\"(?i)(<script>|javascript:)\",    # XSS\n    r\"(?i)(\\.\\./|\\.\\.\\\\)\",            # LFI/Directory Traversal\n    r\"(?i)(/etc/passwd|/wp-admin)\",   # Common Scanning Paths\n    r\"(?i)(cmd=|exec=|system\\()\"      # RCE Attempts\n]\n\ndef parse_log_line(line):\n    # Apache Combined Log Format Regex\n    pattern = r'^(\\S+) (\\S+) (\\S+) \\[([\\w:/]+\\s[+\\-]\\d{4})\\] \"(\\S+) (\\S+) (\\S+)\" (\\d{3}) (\\d+) \"(\\S+)\" \"(\\S+)\"'\n    match = re.match(pattern, line)\n    if match:\n        return {\n            'ip': match.group(1),\n            'timestamp': match.group(4),\n            'method': match.group(5),\n            'path': match.group(6),\n            'protocol': match.group(7),\n            'status': int(match.group(8)),\n            'size': match.group(9),\n            'referrer': match.group(10),\n            'user_agent': match.group(11)\n        }\n    return None\n\ndef analyze_logs(filepath):\n    ip_stats = defaultdict(lambda: {'requests': 0, 'errors': 0, 'attacks': []})\n    suspicious_ips = set()\n\n    try:\n        with open(filepath, 'r', encoding='utf-8', errors='ignore') as f:\n            for line in f:\n                data = parse_log_line(line.strip())\n                if not data:\n                    continue\n\n                ip = data['ip']\n                ip_stats[ip]['requests'] += 1\n\n                # Cek Error Codes (4xx & 5xx)\n                if data['status'] >= 400:\n                    ip_stats[ip]['errors'] += 1\n\n                # Cek Payload Serangan\n                for pattern in ATTACK_PATTERNS:\n                    if re.search(pattern, data['path'] + data['user_agent']):\n                        ip_stats[ip]['attacks'].append({\n                            'type': pattern,\n                            'path': data['path'],\n                            'status': data['status']\n                        })\n                        suspicious_ips.add(ip)\n                        break\n\n        # Evaluasi Threshold\n        print(f\"[*] Memulai Analisis Keamanan: {filepath}\")\n        print(\"-\" * 60)\n        \n        for ip, stats in ip_stats.items():\n            is_suspicious = False\n            reasons = []\n\n            if stats['requests'] > THRESHOLD_REQUESTS:\n                is_suspicious = True\n                reasons.append(f\"High Traffic ({stats['requests']} reqs)\")\n            \n            if stats['errors'] > THRESHOLD_ERRORS:\n                is_suspicious = True\n                reasons.append(f\"High Error Rate ({stats['errors']} errors)\")\n            \n            if ip in suspicious_ips:\n                is_suspicious = True\n                reasons.append(f\"Attack Signature Detected ({len(stats['attacks'])} attempts)\")\n\n            if is_suspicious:\n                print(f\"[ALERT] IP: {ip}\")\n                print(f\"        Reason: {', '.join(reasons)}\")\n                if stats['attacks']:\n                    print(f\"        Sample Attack: {stats['attacks'][0]['path']}\")\n                print(\"-\" * 60)\n\n    except FileNotFoundError:\n        print(f\"[ERROR] File log '{filepath}' tidak ditemukan.\")\n        sys.exit(1)\n\nif __name__ == \"__main__\":\n    # Gunakan argumen CLI jika ada,\u5426\u5219 pakai default\n    target_log = sys.argv[1] if len(sys.argv) > 1 else LOG_FILE\n    analyze_logs(target_log)\n```\n\n### Mekanisme Deteksi\n\n1.  **Volume Anomaly:** IP yang melakukan request melebihi `THRESHOLD_REQUESTS` mengindikasikan potensi DDoS atau Bruteforce.\n2.  **Error Rate:** Tingginya kode status 4xx/5xx menandakan aktivitas scanning directory atau vulnerability probing.\n3.  **Signature Matching:** Regex mendeteksi pola payload berbahaya pada URL atau User-Agent (SQLi, XSS, LFI, RCE).\n\n### Aksi Mitigasi (Wajib Lakukan)\n\nJangan hanya menganalisis. Setelah menemukan IP mencurigakan:\n\n1.  **Blokir via Firewall:**\n    ```bash\n    iptables -A INPUT -s <SUSPICIOUS_IP> -j DROP\n    ```\n2.  **Integrasi Fail2Ban:**\n    Buat filter custom di `/etc/fail2ban/filter.d/apache-shiro.conf` menggunakan regex di atas, lalu aktifkan jail-nya.\n3.  **WAF Rule:**\n    Jika menggunakan Cloudflare atau ModSecurity, masukkan pola serangan ke dalam rule blocklist.\n\n### Catatan Keamanan\n*   **False Positives:** Review manual diperlukan sebelum memblokir IP secara permanen, terutama untuk traffic tinggi dari CDN atau crawler legit (Googlebot).\n*   **Performa:** Untuk log berukuran GB+, gunakan `grep` awal untuk filtering atau proses secara streaming agar tidak memakan RAM berlebih.\n*   **Legalitas:** Pastikan Anda memiliki otoritas penuh atas server yang diaudit.\n\nJalankan skrip:\n```bash\npython3 apache_security_audit.py /var/log/apache2/access.log\n```", "model": "DO-GenAI-397B", "timestamp": "20260528_083833", "cycle": 573}
